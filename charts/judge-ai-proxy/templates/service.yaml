apiVersion: v1
kind: Service
metadata:
  name: {{ include "judge-ai-proxy.fullname" . }}
  labels:
    {{- include "judge-ai-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "judge-ai-proxy.selectorLabels" . | nindent 4 }}