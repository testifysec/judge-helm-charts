# Patterns to ignore when building packages.
# This supports shell glob matching, relative path matching, and
# negation (prefixed with !). Only one pattern per line.
.DS_Store

# Common VCS dirs
.git/
.gitignore
.bzr/
.bzrignore
.hg/
.hgignore
.svn/

# Common backup files
*.swp
*.bak
*.tmp
*.orig
*~

# Various IDEs
.project
.idea/
*.tmproj
.vscode/

# Build artifacts
app/preview-router
app/*.exe
app/*.dll
app/*.so
app/*.dylib
app/*.test
app/*.out

# Docker files (not needed in Helm chart)
Dockerfile
.dockerignore

# Documentation
BUILD.md
README.md

# Test files
test/
*.test
*_test.go

# Go build cache
app/vendor/
app/go.sum.bak